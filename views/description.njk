{% extends "layout.njk" %}

{% block head %}
    <title>{{item.title}} Alef Santos</title>
{% endblock %}

{% block content %}
    <div id="wrapper">
        <h1>{{item.title}}</h1>
    </div>
    <section id="description">
        <div class="info" id="{{item.id}}">
            <p>{{ item.description }}</p>
            <a href="{{item.link}}" target="_blank">GitHub</a> 
        </div>
        
    </section>
{% endblock %}